# NeoEssentials Permissions Configuration
# Prefix/suffix system for roles, users, and per-world overrides

[roles.admin]
prefix = "[Admin] "
suffix = ""

[roles.mod]
prefix = "[Mod] "
suffix = " (on duty)"

[roles._DEFAULT_]
prefix = ""
suffix = ""

[overrides.world1]
roles._DEFAULT_.suffix = " (lobby)"

[users.123e4567-e89b-12d3-a456-426614174000]
prefix = "[VIP] "
suffix = " ★"
